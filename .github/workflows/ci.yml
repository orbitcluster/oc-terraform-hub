---
name: "hub-ci"

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  issues: write

jobs:
  hub-ci:
    uses: "orbitcluster/oc-terraform-module-eks-setup/.github/workflows/main.yml@v1.7.0"
    with:
      tfvar_file_path: "hub.tfvars"
      bucket_name: "oc-backend-hub"
      approvers: "dilipsamanta, mav-sk"
      cluster_name: "oc-eks-hub"
      module_ref: "v1.7.0"
    secrets: inherit
#
